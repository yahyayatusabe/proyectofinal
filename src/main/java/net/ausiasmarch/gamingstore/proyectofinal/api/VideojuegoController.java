/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package net.ausiasmarch.gamingstore.proyectofinal.api;

import javax.servlet.http.HttpSession;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
/*
@RestController
@RequestMapping("/producto")
public class VideojuegoController {

    @Autowired
    HttpSession oHttpSession;

    @Autowired
    VideojuegoRepository oVideojuegoRepository;

    

    @Autowired
    FillService oFillService;*/
    /*
     @GetMapping("/{id}")
    public ResponseEntity<?> get(@PathVariable(value = "id") Long id) {
        if (oVideojuegoRepository.existsById(id)) {
                    return new ResponseEntity<>(oVideojuegoRepository.getOne(id), HttpStatus.OK);
                } else {
                    return new ResponseEntity<>(oVideojuegoRepository.getOne(id), HttpStatus.NOT_FOUND);
                }
       
        
    }
}*/
